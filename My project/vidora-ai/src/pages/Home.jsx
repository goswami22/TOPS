/**
 * TEMPORARY placeholder — verifies routing/layout foundation only.
 * Replace with the real landing page in a later module.
 */
function Home() {
  return (
    <div className="flex min-h-screen items-center justify-center px-6 text-center">
      <div>
        <h1 className="text-2xl font-semibold text-[rgb(var(--color-text))]">
          Vidora AI — Home Route Active
        </h1>
        <p className="mt-2 text-sm text-[rgb(var(--color-text-muted))]">
          Temporary dev placeholder — routing is working correctly.
        </p>
      </div>
    </div>
  );
}

export default Home;
